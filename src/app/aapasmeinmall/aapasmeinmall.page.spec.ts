import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AapasmeinmallPage } from './aapasmeinmall.page';

describe('AapasmeinmallPage', () => {
  let component: AapasmeinmallPage;
  let fixture: ComponentFixture<AapasmeinmallPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AapasmeinmallPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
