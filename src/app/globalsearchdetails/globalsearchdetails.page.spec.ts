import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GlobalsearchdetailsPage } from './globalsearchdetails.page';

describe('GlobalsearchdetailsPage', () => {
  let component: GlobalsearchdetailsPage;
  let fixture: ComponentFixture<GlobalsearchdetailsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GlobalsearchdetailsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
